from pymongo import MongoClient
import mysql.connector
import _json

client = MongoClient('localhost', 27017)

db = client.huwebshop
productsCollection = db.products

print(db)
print(productsCollection)
print("")

mydb = mysql.connector.connect(
    host="localhost",
    user="root",
    passwd="11326",
    db="huwebshop"
)
print(mydb)
print("")


def migrate():
    collection = productsCollection.find()
    for i in collection:
        _id = i.get("_id")
        productName = i.get("name")
        price = i["price"]["mrsp"]
        sql = "insert into client_test(_id, productName, price) values"+' ('+str(_id)+", ' "+
        print(_id, productName, price)
        print("")
        Print



migrate()
